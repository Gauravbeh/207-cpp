//size of program
#include<iostream.h>
#include<conio.h>
void main()
{
	int a;
	char b[50];
	float c;
	double d;

	clrscr();

	cout<<"the size is int = "<<sizeof(a)<<endl;
	cout<<"the size is char = "<<sizeof(b)<<endl;
	cout<<"the size is float = "<<sizeof(c)<<endl;
	cout<<"the size is double =

	"<<sizeof(d)<<endl;

	getch();
}